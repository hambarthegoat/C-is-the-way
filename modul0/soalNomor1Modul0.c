#include <stdio.h>

int main()
{
    // Inisialisasi
    int A, B, C;

    // Input nilai
    scanf("%d %d", &A, &B);

    // Operasi aritmatika
    C = B - A;

    // Output
    printf("%d", C);
    return 0;
}