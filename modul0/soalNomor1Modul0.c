#include <stdio.h>

int main () {
    // Inisialisasi
    int A, B, C;

    // Input nilai
    scanf("%d %d", &A, &B);

    // Operasi aritmatika
    C = A - B;

    // Output
    printf("%d", C);
    return 0;
}